import { router } from 'expo-router'; import { StyleSheet, Text, View } from 'react-native';
import { PrimaryButton } from '@/components/PrimaryButton'; import { Screen } from '@/components/Screen'; import { colors } from '@/constants/theme'; import { useAppData } from '@/context/AppDataContext'; import { getLevel } from '@/domain/exercises';
export default function WorkoutTab() { const { profile } = useAppData(); const level = getLevel(profile.currentLevel); return <Screen><Text style={styles.title}>오늘의 운동</Text><Text style={styles.level}>Level {level.level} · {level.name}</Text><View style={styles.list}>{level.exercises.map((e,i)=><View key={e.id} style={styles.row}><Text style={styles.index}>{i+1}</Text><View style={{flex:1}}><Text style={styles.name}>{e.name}</Text><Text style={styles.target}>{e.targetText} · {e.totalSets}세트</Text></View></View>)}</View><PrimaryButton title="운동 시작" onPress={()=>router.push('/workout/session')} /></Screen>; }
const styles=StyleSheet.create({title:{fontSize:30,fontWeight:'800',color:colors.text,marginTop:8},level:{fontSize:17,color:colors.primary,fontWeight:'700',marginTop:8,marginBottom:24},list:{flex:1},row:{flexDirection:'row',gap:14,paddingVertical:18,borderTopWidth:1,borderColor:colors.border,alignItems:'center'},index:{width:32,height:32,textAlign:'center',textAlignVertical:'center',borderRadius:16,backgroundColor:colors.accent,color:colors.primary,fontWeight:'800'},name:{fontSize:18,fontWeight:'700',color:colors.text},target:{fontSize:14,color:colors.muted,marginTop:4}});
