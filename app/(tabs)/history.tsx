import { Ionicons } from '@expo/vector-icons'; import { router } from 'expo-router'; import { Pressable, StyleSheet, Text, View } from 'react-native';
import { Screen } from '@/components/Screen'; import { colors } from '@/constants/theme'; import { useAppData } from '@/context/AppDataContext'; import { formatKoreanDate } from '@/utils/date';
export default function HistoryScreen(){const {sessions}=useAppData();return <Screen><Text style={styles.title}>운동 기록</Text>{sessions.length===0?<View style={styles.empty}><Ionicons name="calendar-outline" size={42} color={colors.disabled}/><Text style={styles.emptyTitle}>아직 운동 기록이 없습니다</Text><Text style={styles.emptyCopy}>첫 운동을 완료하면 여기에 표시됩니다.</Text></View>:<View style={styles.list}>{sessions.map(s=><Pressable key={s.id} style={styles.row} onPress={()=>router.push({pathname:'/history/[id]',params:{id:s.id}})}><View><Text style={styles.date}>{formatKoreanDate(s.completedAt)}</Text><Text style={styles.detail}>Level {s.level} · {s.fullyCompleted?'전체 완료':'일부 완료'}</Text></View><Ionicons name="chevron-forward" size={22} color={colors.muted}/></Pressable>)}</View>}</Screen>}
const styles=StyleSheet.create({title:{fontSize:30,fontWeight:'800',color:colors.text,marginTop:8,marginBottom:24},list:{borderTopWidth:1,borderColor:colors.border},row:{minHeight:78,flexDirection:'row',justifyContent:'space-between',alignItems:'center',borderBottomWidth:1,borderColor:colors.border},date:{fontSize:18,fontWeight:'700',color:colors.text},detail:{fontSize:14,color:colors.muted,marginTop:6},empty:{flex:1,alignItems:'center',justifyContent:'center',paddingBottom:80},emptyTitle:{fontSize:19,fontWeight:'700',color:colors.text,marginTop:16},emptyCopy:{fontSize:15,color:colors.muted,marginTop:7}});
