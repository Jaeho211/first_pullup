# First Pull-up 작업 지침

이 문서는 프로젝트를 수정하는 에이전트와 기여자를 위한 기준이다. 프로젝트 소개, 설치법, 기술 구성은 `README.md`에서 관리하며 여기에서 반복하지 않는다.

## 제품 원칙

- 목표는 사용자가 반동 없는 정자세 풀업 1회를 준비하도록 돕는 것이다.
- 앱은 사용자가 선택한 단계의 운동을 안내하고 수행 기록을 제공한다. 단계를 자동 판정하거나 자동 변경하지 않는다.
- 서서 턱 당기기는 목과 머리의 중립 정렬을 위한 준비 운동이다. 풀업의 주운동이나 단독 달성 수단으로 설명하지 않는다.
- 첫 풀업 달성, 운동 효과 또는 달성 기간을 보장하거나 단정하지 않는다.

## 기준 파일

| 영역 | 기준 |
| --- | --- |
| 운동 순서, 목표량, 세트, 설명 | `src/domain/exercises.ts` |
| 프로필과 운동 기록 타입 | `src/domain/types.ts` |
| 세션 진행 및 완료 조건 | `app/workout/session.tsx` |
| 로컬 저장 동작 | `src/services/storage.ts` |
| JSON 내보내기 형식 | `src/services/exportWorkoutData.ts` |

운동 데이터나 앱 동작을 바꿀 때는 코드와 이 문서의 해당 계약을 같은 변경에서 갱신한다.

## 현재 운동 데이터

모든 단계는 서서 턱 당기기 8회 × 1세트와 벽 W 슬라이드 8~10회 × 1세트로 준비한다. 벽 견갑 푸시업은 현재 루틴에서 사용하지 않는다.

| 단계 | 이름 | 준비 운동 이후 구성 |
| --- | --- | --- |
| 1 | 보조 당기기 적응 | 발 보조 매달리기 15~20초 × 3세트 → 의자 보조 견갑 풀업 5회 × 3세트 → 발 보조 풀업 5회 × 3세트 |
| 2 | 견갑과 보조 풀업 | 일반 매달리기 20~30초 × 3세트 → 견갑 풀업 5~8회 × 3세트 → 발 보조 풀업 6~8회 × 3세트 |
| 3 | 네거티브 입문 | 일반 매달리기 20~30초 × 2세트 → 견갑 풀업 6~10회 × 3세트 → 발 보조 풀업 5~8회 × 3세트 → 네거티브 풀업 3회·3초 하강 × 2세트 |
| 4 | 네거티브 강화 | 견갑 풀업 6~10회 × 2세트 → 발 보조 풀업 5~8회 × 3세트 → 네거티브 풀업 3~5회·3~5초 하강 × 3세트 → 풀업 상단 버티기 5~10초 × 2세트 |
| 5 | 첫 풀업 시도 | 보조 없는 풀업 1회씩 × 3시도 → 발 보조 풀업 4~6회 × 3세트 → 네거티브 풀업 3회·5초 하강 × 2세트 → 견갑 풀업 6~8회 × 2세트 |
| 6 | 정자세 풀업 안정화 | 정자세 풀업 1~3회 × 3세트 → 발 보조 풀업 5~8회 × 2세트 → 일반 매달리기 20~30초 × 2세트 |

기본 프로필은 이름 `이재호`, 주간 목표 3회, Level 1이다. 이름, 주간 목표 1~7회, 현재 단계는 설정에서 사용자가 변경한다.

## 동작 계약

- 운동 세션은 세션 당시 운동의 ID, 이름, 목적, 목표, 세트 수, 기록 필드를 스냅샷으로 보존한다. 현재 UI는 완료한 세트와 각 완료 시각을 기록하며, 반복 횟수 입력과 자동 휴식 타이머는 없다.
- 각 세트는 `measurements`에 반복 수, 수행 시간, 하강 시간, 보조 정도 등 운동별 값을 추가할 수 있는 구조다. 새 측정값을 도입할 때는 운동 정의의 `tracking.fields`와 함께 추가한다.
- 사용자는 이전·다음 운동으로 이동할 수 있다.
- 마지막 운동의 모든 세트를 완료하면 세션을 저장한다. 앞선 운동에 미완료 세트가 있으면 `fullyCompleted`는 `false`가 된다.
- 완료 화면에서 체감 강도 `easy`, `moderate`, `hard`를 선택적으로 기록할 수 있다.
- 홈의 오늘 완료 표시는 오늘 저장된 세션의 존재를 기준으로 한다. 월간 달력 표시는 `fullyCompleted` 세션만 사용한다.
- 프로필과 세션은 AsyncStorage에 저장한다. 초기화 후에는 기본 프로필을 다시 생성한다.
- 세션 저장 스키마는 버전 2다. 이전 형식의 `exerciseId`, 완료 세트 수와 완료 시각은 로드할 때 운동 스냅샷과 세트 배열로 마이그레이션한다.
- JSON 공유에는 스키마 버전, 내보낸 시각, 분석 안내, 프로필, 운동 카탈로그, 전체 세션을 포함한다.

## 자세와 안전 기준

- 모든 동작은 반동 없이 통제 가능한 범위에서 수행하도록 안내한다.
- 매달리기는 팔꿈치를 펴고 턱을 앞으로 내밀지 않으며, 그립이 완전히 풀릴 때까지 버티지 않는다.
- 발 보조 풀업은 안정된 지지물과 충분한 발 보조를 사용한다.
- 네거티브 풀업은 의자로 상단 자세를 만든 뒤 통제해서 내려온다.
- 정자세 풀업은 통증이 없고 안정된 자세에서만 시도한다.
- 목이나 어깨의 날카로운 통증, 팔 저림 또는 힘 빠짐이 나타나면 중단하도록 안내한다.
- 목표 세트나 반복을 채우기 위해 자세 또는 안전을 희생하도록 유도하지 않는다.
- 의료 진단, 치료 또는 재활 효과를 암시하는 문구를 추가하지 않는다.

## 변경 규칙

- 운동의 ID는 저장된 세션과 연결되므로 변경 또는 삭제 전에 기존 데이터 호환성을 검토한다.
- `UserProfile`이나 `WorkoutSession` 저장 형식을 바꾸면 이전 AsyncStorage 데이터의 마이그레이션 또는 안전한 기본값 처리를 마련한다.
- 완료 판정이나 세션 저장 시점을 바꾸면 홈 달력, 오늘 완료 표시, 완료 화면, 기록 상세, JSON 내보내기를 함께 확인한다.
- 사용자 안내 문구는 실제 구현된 기능만 설명해야 한다.
- 기존 사용자 변경을 임의로 되돌리지 않으며, 요청 범위 밖의 파일은 수정하지 않는다.

## 완료 전 확인

코드 변경이 있으면 최소한 다음 검사를 실행한다.

```bash
npm run typecheck
npm run lint
```

문서만 변경한 경우 검사를 생략할 수 있지만, 운동 데이터 표와 `src/domain/exercises.ts`의 일치 여부는 직접 확인한다.
